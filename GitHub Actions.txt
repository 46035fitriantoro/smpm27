Login ke GitHub. https://github.com
46035fitriantoro@gmail.com
Klik New Repository.
Misalnya beri nama:
barokahmart
Pilih Private (atau Public jika ingin).
Jangan centang README.
Klik Create Repository.

Install Git

Upload Project
Masuk ke project:
cd D:\laragon\www\flutter
Lalu:
git init
git add .
git config --global user.email "46035fitriantoro@gmail.com"
git config --global user.name "Agus Fitriantoro"
git commit -m "First Commit"
Tambahkan repository GitHub
Misalnya
git remote add origin https://github.com/46035fitriantoro/barokahmart.git
Lalu
git branch -M main
git push -u origin main

Buat Folder Workflow
Di project buat folder
.github
di dalamnya
workflows
Sehingga menjadi
.github
└── workflows

Buat File
Misalnya
android-release.yml
Isi file tersebut:
name: Build Android Release
on:
  push:
    branches:
      - main
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: 3.44.6
          channel: stable
          cache: true
      - name: Flutter Version
        run: flutter --version
      - name: Get Packages
        run: flutter pub get
      - name: Build APK
        run: flutter build apk --release
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-release
          path: build/app/outputs/flutter-apk/app-release.apk

Commit lagi
git add .
git commit -m "Add GitHub Actions"
git push

git add .
git commit -m "Increase Gradle memory for GitHub Actions"
git push

Buka GitHub
Masuk ke
Actions
Nanti muncul
Build Android Release
Klik
Run workflow
Server GitHub akan:
✅ Download Flutter
✅ Download Android SDK
✅ Download dependency
✅ Build Release

Kalau selesai akan muncul
Artifacts
app-release
Klik
Download
Maka APK Release akan terdownload.
Keuntungan
Laptop Anda hanya menjalankan
VS Code
dan
git push
Tidak perlu lagi:
Build Gradle
Compile R8
DEX
Shrink Resources
Semua dilakukan server GitHub.

Saya punya saran yang lebih bagus lagi
Daripada hanya membuat APK, saya bisa membuat workflow yang jauh lebih lengkap. Misalnya setiap git push, GitHub akan:
✅ Build APK Debug.
✅ Build APK Release.
✅ Build Android App Bundle (.aab) untuk Google Play.
✅ Menyimpan semua hasil sebagai artifact.
✅ Memberi nama file berdasarkan versi aplikasi, misalnya BarokahMart-v1.0.0+1.apk.
✅ Otomatis membuat GitHub Release setiap kali Anda membuat tag versi (misalnya v1.0.0), sehingga APK bisa langsung diunduh dari halaman Releases.
Workflow seperti ini lebih profesional dan cocok untuk pengembangan jangka panjang. Saya menyarankan kita langsung membuat versi tersebut daripada workflow yang paling sederhana, supaya nanti Anda tidak perlu mengubahnya lagi ketika aplikasi sudah siap dipublikasikan.

Nanti kalau ada laptop baru, komputer rusak, atau ingin bekerja dari tempat lain, Anda tinggal:

git clone
flutter pub get
git push

dan semuanya langsung berjalan tanpa perlu mengatur ulang dari nol.

=====================================================================================================================

Cara Pertama Kali Upload Project Flutter ke GitHub
1. Masuk ke folder project
cd D:\laragon\www\flutter

2. Inisialisasi Git
git init

3. Tambahkan semua file
git add .

4. Atur nama dan email Git (cukup sekali seumur hidup di komputer tersebut)
git config --global user.name "Agus Fitriantoro"
git config --global user.email "46035fitriantoro@gmail.com"

Cek hasilnya

git config --global --list

5. Commit pertama
git commit -m "Initial Commit"

6. Hubungkan ke Repository GitHub

Misalnya repository:

https://github.com/46035fitriantoro/barokahmart.git

Maka jalankan

git remote add origin https://github.com/46035fitriantoro/barokahmart.git

7. Ganti branch menjadi main
git branch -M main

8. Upload ke GitHub
git push -u origin main

Selesai.

Cara Kedua dan Seterusnya (Update Project)

Misalnya Anda sudah menambah fitur baru.

1. Masuk ke project
cd D:\laragon\www\flutter

2. Cek perubahan
git status

3. Tambahkan perubahan
git add .

4. Commit

Contoh

git commit -m "Menambah fitur login"

atau

git commit -m "Perbaiki bug transaksi"

5. Upload ke GitHub
git push

Selesai.

Cara Melihat Repository GitHub yang Terhubung
git remote -v

Hasilnya misalnya

origin  https://github.com/46035fitriantoro/barokahmart.git (fetch)
origin  https://github.com/46035fitriantoro/barokahmart.git (push)
Kalau Salah Repository

Hapus remote lama

git remote remove origin

Tambahkan lagi

git remote add origin https://github.com/46035fitriantoro/barokahmart.git
Kalau Lupa Sudah Pernah Commit atau Belum

Lihat riwayat commit

git log --oneline

Contoh

8f5d2aa Initial Commit
4d92fba Menambah Login
19abcc1 Perbaiki Bug
Alur Kerja Sehari-hari
Coding
   │
   ▼
git status
   │
   ▼
git add .
   │
   ▼
git commit -m "Keterangan perubahan"
   │
   ▼
git push
   │
   ▼
GitHub
   │
   ▼
GitHub Actions Build APK
   │
   ▼
Download APK
Tips tambahan

Untuk project Flutter, pastikan file .gitignore ada di root project. File ini biasanya sudah dibuat otomatis oleh Flutter dan akan mencegah folder seperti build/, .dart_tool/, .gradle/, dan file sementara lainnya ikut ter-upload ke GitHub. Ini membuat repository tetap bersih dan proses git push lebih cepat.

Setelah project berhasil di-push, kita lanjut membuat GitHub Actions agar setiap git push otomatis membangun APK Release dan AAB tanpa perlu build di laptop Anda.